<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wang.fastfood.usercenter.dao.UserDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wang.fastfood.usercenter.entity.PO.UserPO">
        <id column="id" property="id" />
        <result column="uid" property="uid" />
        <result column="user_name" property="username" />
        <result column="password" property="password"/>
        <result column="phone_number" property="phoneNumber" />
        <result column="sex" property="sex" />
        <result column="birthday" property="birthday" />
        <result column="join_time" property="joinTime" />
        <result column="user_level" property="userLevel" />
        <result column="experience" property="experience" />
        <result column="valid" property="valid" />
        <result column="crate_time" property="crateTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    <insert id="add">
        insert into user
            (id, uid, user_name, password, user_level, experience, valid)
            VALUES
            (#{User.id},#{User.uid},#{User.username},#{User.password},#{User.userLevel},#{User.experience},1);
    </insert>
    <select id="getByUserName" resultMap="BaseResultMap">
        select * from user where user_name = #{userName}
    </select>

</mapper>
